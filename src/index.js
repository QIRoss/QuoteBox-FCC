import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Box from './Box';

ReactDOM.render(
  <React.StrictMode>
    <Box />
  </React.StrictMode>,
  document.getElementById('root')
);